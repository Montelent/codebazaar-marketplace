/**
 * Prisma was removed. Admin is created at runtime via SQL when needed.
 * This file is kept only so the path still exists; it is excluded from the Next.js build.
 */
console.log("Prisma seed disabled. Use ADMIN_EMAIL / ADMIN_PASSWORD env vars.");
